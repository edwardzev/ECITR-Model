#!/usr/bin/env bash
set -euo pipefail

ECITR_MODEL_ROOT="${ECITR_MODEL_ROOT:-${HOME}/ECITR-Model}"
CLI="${ECITR_MODEL_ROOT}/src/cli/record-memory-usage.js"

if [[ ! -f "${CLI}" ]]; then
  printf 'ECITR usage CLI not found: %s\n' "${CLI}" >&2
  exit 1
fi

exec node "${CLI}" "$@"
